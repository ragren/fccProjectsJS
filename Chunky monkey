function chunkArrayInGroups(arr, size) {
i=size;
a = 0;   
var array =[];  
while(a<arr.length){
  array.push(arr.slice(a,i));
  a+=size;
  i+=size;
}    
return array;
}

chunkArrayInGroups([0, 1, 2, 3, 4, 5, 6], 3);
