function chunkArrayInGroups(arr, size) {
var array =[];  
for(start=0, end=size; start<arr.length; start+=size, end+=size){
  array.push(arr.slice(start,end));
}    
return array;
}
chunkArrayInGroups([0, 1, 2, 3, 4, 5, 6], 3);
